pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: npx biome check --write {staged_files}
      stage_fixed: true

    - name: typecheck
      run: pnpm typecheck

    - name: test
      run: pnpm test
