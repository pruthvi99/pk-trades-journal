# Nixpacks build configuration for Railway.
# Adds Python 3 so node-gyp can compile better-sqlite3 native bindings.

[phases.setup]
nixPkgs = ["nodejs", "nodePackages.pnpm", "python3", "gnumake", "gcc"]
